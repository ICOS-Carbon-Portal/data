# DB specific
all_cols = 'ip,latitude,longitude,continent_code,continent_name,country_code,country_name,region_code,region_name,city,zip,is_eu'.split(',')
min_cols = 'ip'.split(',')
default_cols = 'ip,country_code'.split(',')

# External IP resolver (ipstack.com)
access_key = 'secret'
