akka.http.server.parsing.max-content-length = 128g

cpdata{
	auth = ${cpauth.auth.pub} //substituted from cpauth core config
	netcdf {
		folder = "/disk/data/common/netcdf/dataDemo/"
		dateVars = ["date", "time", "tstep"]
		latitudeVars = ["latitude", "lat"]
		longitudeVars = ["longitude", "lon"]
		elevationVars = ["nz"]
	}
	meta {
		//cpauthToken = "secure token must be provided in deployment-specific application.conf"
		url = "http://127.0.0.1:9094/sparql"
	}
	upload {
		folder: "./fileStorage"
		irods {
			host: "130.237.230.190"
			port: 1247
			username: "oleg"
			password: "password"
			homeDirectory: "/eudat.se/projects/icos"
			zone: "eudat.se"
			defaultResource: "EUDAT"
		}
	}
}

